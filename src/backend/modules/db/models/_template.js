module.exports = [
    // Структура
    {

    },

    // Настройки
    {
        versionKey: false
    },

    // Индексы (опц.): элемент — либо объект определения { field: 1 | '2dsphere' | ... },
    // либо кортеж [definition, options] для индекса с опциями.
    []
]
